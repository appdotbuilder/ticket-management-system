import { type ClosingReason } from '../schema';

export async function getClosingReasons(): Promise<ClosingReason[]> {
    // This is a placeholder declaration! Real code should be implemented here.
    // The goal of this handler is fetching all active closing reasons.
    return Promise.resolve([] as ClosingReason[]);
}